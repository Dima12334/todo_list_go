package main

const configDir = "configs"

func main() {
	//cfg, _ := config.Init(configDir)
	//srv := server.NewServer(cfg)
	//
	//if err := srv.Run(); err != nil {
	//	log.Fatal(err.Error())
	//}
}
